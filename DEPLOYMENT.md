# 🚀 Развёртывание на Bothost.ru

## Быстрый старт

### Способ 1: Через Git (рекомендуется)

1. **Запушьте код на GitHub/GitLab**
   ```bash
   git add .
   git commit -m "Подготовка для bothost.ru"
   git push origin main
   ```

2. **Подключите репозиторий на Bothost**
   - Зарегистрируйтесь на [bothost.ru](https://bothost.ru)
   - Создайте новый проект
   - Выберите тип "Telegram Bot"
   - Подключите ваш GitHub/GitLab репозиторий
   - Выберите ветку `main`

3. **Настройте переменные окружения**
   
   В панели Bothost добавьте переменные:
   | Переменная | Значение |
   |------------|----------|
   | `TELEGRAM_BOT_TOKEN` | Токен от @BotFather |
   | `TELEGRAM_ADMIN_ID` | Ваш ID админа |
   | `DATABASE_PATH` | `/app/data/booking.db` |
   | `TIMEZONE` | `Europe/Moscow` |

4. **Задеплойте**
   - Нажмите "Deploy"
   - Bothost автоматически соберёт и запустит бота

---

### Способ 2: Через Dockerfile

Проект уже содержит `Dockerfile` для надёжного развёртывания.

1. **Подготовьте файлы**
   - Убедитесь, что все файлы в репозитории
   - `.env` файл **не** загружайте (токены через панель Bothost)

2. **На Bothost**
   - Создайте проект
   - Выберите "Docker" как тип развёртывания
   - Подключите репозиторий
   - Настройте переменные окружения (см. выше)

---

## 📋 Чек-лист перед загрузкой

- [ ] Скопируйте `.env.example` в `.env` локально
- [ ] Добавьте токен бота и admin ID в `.env`
- [ ] Настройте услуги в `config.py`
- [ ] **Не загружайте `.env` в репозиторий!**
- [ ] Токены добавьте через панель Bothost

---

## 🔧 Настройка услуг

Отредактируйте `config.py` перед загрузкой:

```python
SERVICES = {
    "haircut": {
        "name": "✂️ Стрижка",
        "duration": 60,
        "price": 1500,
        "description": "Мужская стрижка с мытьём головы"
    },
    "beard": {
        "name": "🧔 Оформление бороды",
        "duration": 45,
        "price": 1000,
        "description": "Оформление бороды и усов"
    },
    # Добавьте свои услуги
}
```

---

## 📊 Тарифы Bothost

| Тариф | Цена | Боты | CPU | RAM | SSD |
|-------|------|------|-----|-----|-----|
| **Starter** | Бесплатно | 1 | 1 vCPU | 256 MB | 0.5 GB |
| **Basic** | 99₽/мес | 5 | 2 vCPU | 1 GB | 5 GB |
| **Pro** | 250₽/мес | 20 | 4 vCPU | 2 GB | 15 GB |

> ⚠️ **Важно:** На бесплатном тарифе данные сбрасываются при рестарте. Для продакшена используйте Basic или Pro.

---

## 🗄 База данных

По умолчанию используется SQLite (`booking.db`).

**Для Basic/Pro тарифов:**
- База данных сохраняется в `/app/data/booking.db`
- Данные сохраняются между рестартами

**Для Production (рекомендуется):**
- Используйте управляемую БД от Bothost
- Или подключите внешний PostgreSQL/MySQL

---

## 🔍 Логи и мониторинг

После развёртывания:
- **Логи:** Раздел "Logs" в панели Bothost
- **Мониторинг:** Раздел "Metrics" (CPU, RAM)
- **Перезапуск:** Кнопка "Restart" при необходимости

---

## ❓ Troubleshooting

### Бот не запускается
1. Проверьте логи в панели Bothost
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен
3. Проверьте `requirements.txt`

### База данных не сохраняется
- Убедитесь, что используете Basic или Pro тариф
- Проверьте путь `DATABASE_PATH=/app/data/booking.db`

### Ошибка импортов
```bash
# Локально проверьте:
pip install -r requirements.txt
python bot.py
```

---

## 📞 Поддержка

- Документация Bothost: https://bothost.ru/docs
- Telegram бот поддержки: @bothost_support
